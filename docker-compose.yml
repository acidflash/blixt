services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      - FIRMS_MAP_KEY=${FIRMS_MAP_KEY}
    volumes:
      - strikes-data:/data
    restart: unless-stopped

volumes:
  strikes-data:
