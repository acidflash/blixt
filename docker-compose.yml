services:
  app:
    build:
      context: .
      args:
        VITE_CARTO_API_KEY: ${VITE_CARTO_API_KEY}
    ports:
      - "80:80"
    environment:
      - FIRMS_MAP_KEY=${FIRMS_MAP_KEY}
    volumes:
      - strikes-data:/data
    restart: unless-stopped

volumes:
  strikes-data:
